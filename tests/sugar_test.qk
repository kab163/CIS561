class Pt(x: Int, y: Int) {
     this.x = x;
     this.y = y;

     def _get_x(): Int {
         return this.x;
     }
     def _get_y(): Int {
         return this.y;
     }

     def PLUS(other: Pt): Pt {
         return Pt(this.x + other.x, this.y + other.y);
     }

}
